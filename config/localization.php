<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Content Locales
    |--------------------------------------------------------------------------
    |
    | Which languages translatable content (category names, descriptions,
    | SEO fields, etc.) is authored in — not to be confused with APP_LOCALE,
    | which is the UI language shown to a given visitor. Every locale listed
    | here gets its own translation row (e.g. in `category_translations`).
    | Keyed by locale code, valued by the display label shown in admin forms.
    |
    */

    'locales' => [
        'uk' => 'Українська',
        'en' => 'English',
    ],

];
